# Base URL of the StyleSense AI backend.
# Leave empty to call the same origin (useful behind a reverse proxy).
# Local development:  http://localhost:5000
# Production:         https://stylesense-api.onrender.com
VITE_API_URL=http://localhost:5000
